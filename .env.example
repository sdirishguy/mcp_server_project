# --- Core ---
ENVIRONMENT=development            # development | staging | production
MCP_SERVER_PORT=8000
LOG_LEVEL=INFO

# --- Security / Auth ---
JWT_SECRET=change-me               # set a strong secret in prod/CI
ADMIN_USERNAME=
# Set these in local/dev only. Leave blank in VCS.
ADMIN_PASSWORD=            # change in prod!

# --- CORS (comma-separated origins) ---
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# --- Paths ---
MCP_BASE_WORKING_DIR=./shared_host_folder
AUDIT_LOG_FILE=./logs/audit.log

# --- LLMs (optional) ---
OPENAI_API_KEY=
# Optional LLM keys (leave empty; set via secrets in CI/prod)
GEMINI_API_KEY=
