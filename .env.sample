# Logging
LOG_LEVEL=INFO

# Dev admin bootstrap (for /api/auth/login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# MCP working directory (already used by your tools)
MCP_BASE_DIR=./shared_host_folder

# Optional: default base URL for RestApiAdapter
# REST_ADAPTER_BASE_URL=https://httpbin.org
